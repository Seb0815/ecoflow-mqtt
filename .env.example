# EcoFlow Cloud MQTT Publisher - Konfiguration
# Kopieren Sie diese Datei zu .env und füllen Sie Ihre Werte aus

# === EcoFlow API Konfiguration ===
# Authentifizierungstyp: "private" (Email/Passwort) oder "public" (Access/Secret Key)
ECOFLOW_AUTH_TYPE=private

# Private API Credentials (für private API)
ECOFLOW_USERNAME=ihre-email@beispiel.de
ECOFLOW_PASSWORD=ihr-passwort

# Public API Credentials (für public API)
#ECOFLOW_ACCESS_KEY=ihr-access-key
#ECOFLOW_SECRET_KEY=ihr-secret-key

# API Host (normalerweise nicht ändern)
ECOFLOW_API_HOST=api.ecoflow.com
ECOFLOW_GROUP=default

# === Geräte-Konfiguration ===
# Format: "SN1:DEVICE_TYPE1:NAME1,SN2:DEVICE_TYPE2:NAME2"
# Beispiele für DEVICE_TYPE: DELTA_2, DELTA_PRO, RIVER_2, POWERSTREAM, etc.
ECOFLOW_DEVICES=HW52ZDH4SF270677:DELTA_2:Delta2-Garage,HW53ABC1234567:RIVER_2:River2-Wohnzimmer

# === MQTT Konfiguration (für die Weiterleitung) ===
MQTT_HOST=localhost
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_BASE_TOPIC=ecoflow

# === Abfrage-Intervall ===
# Wie oft sollen die Daten abgefragt werden (in Sekunden)
REFRESH_INTERVAL=30
